= render 'tabs', current_tab: 'Upcoming'
= render partial: "events", locals: { title: "Invited Events", volunteer_events: @invited_events}
= render partial: "events", locals: { title: "Registered Events", volunteer_events: @registered_events }
= render partial: "events", locals: { title: "Approved Events", volunteer_events: @approved_events }

